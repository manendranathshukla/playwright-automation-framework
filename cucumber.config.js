module.exports = {
    require: ['./features/**/*.js'], // Define the path to your step definitions
    format: ['pretty'], // You can add more formatters if needed
    timeout: 30000, // Set a default timeout for steps
};
